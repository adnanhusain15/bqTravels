/** @type {import('next').NextConfig} */
const nextConfig = {
  //   webpack: (config) => {
  //     config.resolve.fallback = {
  //       ...config.resolve.fallback,
  //       net: false,
  //       os: false,
  //       fs: false,
  //       child_process: false,
  //     };
  //     return config;
  //   },
};

export default nextConfig;
